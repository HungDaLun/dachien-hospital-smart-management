# 苗栗大千醫院 Progress Note 與住院摘要 AI 生成系統
## 技術方案建議書

**文件版本**：v2.0
**建立日期**：2026-01-20
**最後更新**：2026-01-20
**適用對象**：苗栗大千醫院
**系統定位**：醫療文書 AI 輔助生成系統

---

## 📋 執行摘要

### 核心問題分析

根據您的描述，目前系統存在以下關鍵問題：

1. **缺乏向量資料庫**：每次生成 Progress note 都要傳送完整病人資料給 OpenAI
2. **效能問題**：傳輸大量資料導致速度慢（每次可能傳送數百 KB 至數 MB）
3. **品質問題**：AI 無法有效理解上下文，生成結果不符合醫生需求
4. **資料隔離疑慮**：擔心所有病人資料混在一起會造成混亂

### 解決方案核心概念

有兩種技術路線可以解決這些問題：

#### 路線一：長上下文模型（快速驗證首選）

**Google Gemini 3** 系列模型支援 **1M token 上下文窗口**（約 75-100 萬中文字），這意味著：
- 單次可傳入單一病患的完整病歷資料
- **不需要向量資料庫**，直接傳入即可
- 適合快速驗證 AI 生成品質

#### 路線二：向量資料庫（長期架構首選）

**向量資料庫（Vector Database）** 是建立長期穩定系統的關鍵技術：
- **語義搜尋**：不需要傳送全部資料，只需搜尋相關片段
- **上下文理解**：AI 能理解醫療概念的語義關聯
- **資料隔離**：透過技術架構確保病人資料完全隔離
- **效能提升**：從數十秒縮短至數秒內完成
- **成本優化**：只傳送必要資料，降低 API 費用

---

## 🎯 需求分析

### 需求一：醫生 Progress Note 自動生成

**使用場景**：
- 醫生查看病人時，需要撰寫 Progress note
- 需要整合：檢查報告、病史、用藥記錄、過敏史、診斷記錄等
- 期望：按一個按鈕，AI 自動生成符合醫療格式的 Progress note

**輸入資料來源**：
- HIS 系統 API（病患基本資料、檢查報告、用藥記錄）
- 歷史病歷記錄
- 影像報告（如 X 光、CT、MRI 等）

**輸出要求**：
- 符合醫療格式的 Progress note
- 包含：主訴、現病史、理學檢查、診斷、處置計畫等
- 必須標註資料來源與引用
- **必須經醫師審核並電子簽章**（法定要求）

### 需求二：護理人員住院摘要生成

**使用場景**：
- 病人在轉院、轉加護、轉普通病房等特定情況下
- 需要生成住院期間的 Summary 摘要
- 期望：按一個按鈕，自動生成摘要供下一位醫護人員參考

**輸入資料來源**：
- 住院期間的每日病情變化記錄
- 護理記錄、生命徵象、用藥記錄
- 檢查報告、診斷變更記錄

**輸出要求**：
- 結構化住院摘要
- 包含：入院原因、住院期間重要事件、目前狀況、用藥調整等
- 時間軸整理，便於快速理解

---

## ⚖️ 台灣醫療法規合規要求

> ⚠️ **重要提醒**：本章節為原始建議書缺漏的關鍵內容

### 相關法規

| 法規 | 要求 | 對系統的影響 |
|------|------|-------------|
| **個人資料保護法** | 病患資料屬於「特種個資」，需要更嚴格的保護措施 | 需取得病患同意或符合法定例外、加密儲存、存取記錄 |
| **醫療法第 72 條** | 病歷保存期限至少 7 年 | AI 生成的 Progress Note 屬於病歷一部分，需長期保存 |
| **電子病歷管理辦法** | 電子病歷需符合電子簽章法要求 | 醫師審核後需進行電子簽章 |
| **醫療機構電子病歷製作及管理辦法** | 電子病歷的製作、儲存、傳輸規範 | 系統需符合衛福部規範 |

### AI 生成內容的法律責任

**關鍵問題**：Progress Note 是**法定病歷**的一部分，若 AI 生成錯誤資訊導致醫療疏失，責任歸屬為何？

**法律見解**：
1. **最終責任在醫師**：AI 僅為「輔助工具」，醫師仍需審核並簽章確認
2. **審核義務**：醫師有義務審閱 AI 生成內容的正確性
3. **追溯記錄**：系統需保留 AI 原始輸出 vs 醫師修改後的版本差異

**系統設計要求**：
- ✅ **強制審核流程**：AI 生成後必須經醫師審核，不可直接寫入病歷
- ✅ **電子簽章**：審核通過後需醫師電子簽章
- ✅ **版本記錄**：保留 AI 原始輸出與醫師修改的完整記錄
- ✅ **稽核軌跡**：記錄誰在何時審核/修改/簽章

---

## 🔌 HIS 系統整合挑戰

> ⚠️ **重要提醒**：這是專案成敗的關鍵因素，原始建議書著墨較少

### HIS 系統現況分析

台灣醫院常見的 HIS 廠商包括：
- 杏翔資訊
- 醫揚科技
- 研華科技
- 衛生福利部醫院（使用公版）
- 其他區域性廠商

### 整合挑戰

| 挑戰 | 描述 | 緩解策略 |
|------|------|---------|
| **API 可用性** | 許多 HIS 系統是封閉系統，API 可能不完整或需額外付費 | 早期與 HIS 廠商溝通，評估 API 授權費用 |
| **資料格式** | 資料格式可能是 HL7、FHIR 或廠商私有格式 | 建立資料轉換層（ETL），支援多種格式 |
| **即時性** | 部分 HIS 系統不支援即時 API，只能批次匯出 | 評估是否可接受資料延遲，或協商即時 API 開發 |
| **安全性要求** | HIS 廠商可能有嚴格的資安審核要求 | 預留資安審核時間（可能 1-3 個月） |

### 建議的整合方式

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│    HIS 系統     │     │   資料轉換層     │     │   AI 生成系統   │
│                 │     │   (ETL Layer)   │     │                 │
│  • 病歷資料     │────▶│  • 格式轉換      │────▶│  • 向量資料庫   │
│  • 檢查報告     │     │  • 資料清理      │     │  • LLM 服務     │
│  • 用藥記錄     │     │  • 去識別化      │     │  • 格式驗證     │
│                 │◀────│  • 寫回轉換      │◀────│                 │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

### HIS 整合成本估算

| 項目 | 成本範圍 (TWD) | 說明 |
|------|---------------|------|
| HIS API 授權費 | $0 - $300,000 | 視廠商政策而定，部分廠商免費，部分需付費 |
| 資料轉換層開發 | $100,000 - $300,000 | 依資料格式複雜度而定 |
| 資安審核配合 | $50,000 - $100,000 | 文件撰寫、測試、審核會議 |
| **總計** | **$150,000 - $700,000** | 這是原始建議書低估的部分 |

---

## 🏗️ 技術架構設計

### 架構總覽

```
┌─────────────────────────────────────────────────────────────┐
│                    系統架構圖                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────┐      ┌──────────────────┐           │
│  │   HIS 系統       │      │   前端介面        │           │
│  │  (資料來源)      │◄─────┤  (醫生/護理站)   │           │
│  └────────┬─────────┘      └────────┬─────────┘           │
│           │                          │                      │
│           │ API 串接                 │ 使用者操作            │
│           ▼                          ▼                      │
│  ┌─────────────────────────────────────────────┐           │
│  │        資料同步與處理層 (ETL Pipeline)        │           │
│  │  • 從 HIS 系統拉取資料                       │           │
│  │  • 資料清理與標準化                          │           │
│  │  • 去識別化處理（如需要）                    │           │
│  └───────────────┬─────────────────────────────┘           │
│                  │                                          │
│                  │ 儲存                                      │
│                  ▼                                          │
│  ┌─────────────────────────────────────────────┐           │
│  │      向量資料庫層 (Vector Database)          │           │
│  │  ┌──────────────┐  ┌──────────────┐         │           │
│  │  │ 病人資料索引 │  │ 醫療知識庫   │         │           │
│  │  │ (隔離儲存)   │  │ (共用知識)   │         │           │
│  │  └──────────────┘  └──────────────┘         │           │
│  └───────────────┬─────────────────────────────┘           │
│                  │                                          │
│                  │ 語義搜尋                                  │
│                  ▼                                          │
│  ┌─────────────────────────────────────────────┐           │
│  │      AI 生成服務層 (LLM Service)             │           │
│  │  • 語義搜尋相關資料片段                       │           │
│  │  • 生成 Progress Note / 住院摘要              │           │
│  │  • 格式驗證與品質檢查                         │           │
│  └───────────────┬─────────────────────────────┘           │
│                  │                                          │
│                  │ 返回結果                                  │
│                  ▼                                          │
│  ┌─────────────────────────────────────────────┐           │
│  │      審核與簽章層 (Review & Sign)            │           │
│  │  • 醫生/護理人員審核（強制）                  │           │
│  │  • 電子簽章確認（法定要求）                   │           │
│  │  • 版本差異記錄（稽核要求）                   │           │
│  │  • 寫回 HIS 系統                             │           │
│  └─────────────────────────────────────────────┘           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 後端資料庫整合策略與限制條件（新增）

> 本節回應「到底要先讓醫院把資料匯入向量資料庫，還是我們直接對接他們資料庫」等核心問題。

#### 一、無論選哪一種方案，都必須先做的盤點工作

- **後端資料庫現況**
  - 資料庫類型：Oracle / SQL Server / MySQL / PostgreSQL / MongoDB… 等
  - 主要資料表：Progress Note、住院摘要、檢驗報告、影像報告、用藥紀錄、過敏史、診斷紀錄等
  - 關聯鍵：病歷號、住院號、就診序號、科別、主治醫師等
- **對外整合能力**
  - 是否已有標準 API（特別是 FHIR / HL7 / REST API）
  - 是否已有 ESB / Integration Engine 可供掛接
  - 若無 API，是否允許 read-only 資料庫連線或批次匯出檔案（CSV / HL7）
- **資安與法規限制**
  - 是否要求所有資料必須留在院內（on‑prem / VPN）
  - 是否允許在雲端儲存「向量化後的資料」但不包含明文病歷
  - 院內資安審查流程與預估時間

> ✅ 結論：**在這些條件釐清之前，不應先要求醫院自行建一個向量資料庫**，而是由我們依既有環境設計最合適的資料流。

#### 二、責任分工：誰維持「原始資料」，誰負責「向量資料庫」

- **醫院既有系統（HIS/EMR/PACS）**
  - 負責：病人所有「原始結構化資料與文件」的真實來源（Source of Truth）
  - 例如：HIS 表格、PACS 影像連結、PDF 報告、醫囑與用藥紀錄等
- **本 AI 系統**
  - 負責：從院方系統「讀取」必要欄位，進行：
    - 資料清理與標準化
    - 向量化與語義索引（向量資料庫）
    - AI 生成、格式化與品質檢查

> ✅ 建議：**不建議由醫院自行先建向量資料庫再讓我們對接**，因為向量庫的 schema、維度、更新策略與我們的 AI 查詢邏輯高度耦合，長期維護風險大。

#### 三、兩種整合架構比較：直接連院方 DB vs. 中介資料庫（推薦）

- **方案 A：直接讀取醫院資料庫（read‑only）**
  - 作法：
    - 由院方開立 read‑only 帳號，限縮 IP 與 schema 權限
    - 本系統在產生 Progress Note / 住院摘要時，直接查詢所需欄位
  - 優點：
    - 實時性佳，醫師儲存後幾乎立即可用
    - 不需要建立額外資料儲存層
  - 缺點：
    - AI 生成時可能大量查詢，容易對 HIS 主資料庫造成負載
    - 不適合做大量向量化批次運算（會佔用 DB 資源）
    - 向量資料庫仍需另建一套，無法只靠傳統 RDB 完成語義搜尋

- **方案 B：建立「中介資料庫 + 向量庫」同步層（推薦）**
  - 作法：
    - 設計一個 ETL / Sync Service：
      - 從 HIS 透過 API / DB read‑only / 批次匯出取回資料
      - 進行欄位轉換、格式整理、必要去識別化
      - 寫入本系統的關聯式資料庫（中介層）與向量資料庫
  - 優點：
    - AI 查詢與向量搜尋只打在我們自己的 DB/向量庫，不直接衝擊 HIS
    - 我們可以完全控制資料 schema、索引與效能調優
    - 便於之後擴充到其他醫院（只需要替換 ETL 介面）
  - 缺點：
    - 需設計可靠的「增量同步機制」，避免延遲與漏資料
    - 初期建置工作較多

> ✅ 綜合評估：**本文件後續所有架構與成本，預設採用方案 B（中介資料庫 + 向量庫）作為長期正式方案。**

#### 四、系統壓力與同時多人使用的承載方式

針對「同一時間很多醫師按下生成 Progress Note 系統會不會撐不住？」的擔憂，技術上採用以下設計：

- **1. 任務佇列（Job Queue）而非同步直呼 AI**
  - 醫師按下「生成」時：
    - 不是直接阻塞等待 AI 回應，而是建立一筆「生成任務」寫入工作佇列（如 RabbitMQ / Redis Queue / SQS）
    - 後端一組 Worker 服務負責依序處理這些任務
  - 效果：
    - 高峰時可能「排隊稍久」，但系統不會因瞬間流量暴增而當機
    - 方便根據實際負載水平擴充 Worker 節點

- **2. 非同步 + 流式生成**
  - Worker 呼叫 Gemini / 其他 LLM 時，採用流式回傳與超時控制：
    - 可邊生成邊寫入暫存，再一次回給前端
    - 若超時，可自動重試或回覆「稍後完成」提示

- **3. 限流與優先權控制**
  - 針對：
    - 緊急病患 / 加護病房 → 可設定較高優先權
    - 大量批次生成（例如自動預生成）→ 排在背景低優先序
  - 避免背景批次任務擠壓即時臨床使用的需求。

#### 五、生成策略：手動生成 vs. 自動預生成

本系統的設計建議採取「分階段」導入，兼顧醫療責任與效能：

- **階段 1：手動觸發 + 強制人工審核（導入期必備）**
  - 流程：
    - 醫師／護理人員在看到最新檢查與病情變化後，按「生成草稿」
    - 系統讀取近期相關資料→向量搜尋→呼叫 LLM 生成結構化草稿
    - 使用者可編輯、刪減、補充內容，最後按「確認送出」並電子簽章
  - 優點：
    - 醫師保有決策與掌控感，較容易被臨床端接受
    - 初期若 AI 偶有不準，風險由人工審核攔截

- **階段 2：自動預生成草稿 + 開啟病歷時已準備好**
  - 流程：
    - 系統偵測到「有新檢驗報告、影像報告、重要醫囑異動」時，自動在背景觸發生成任務
    - 醫師打開該住院個案時，立即看到「系統預先生成的最新版草稿」
    - 醫師僅需微調與簽章，降低等待時間與高峰期壓力
  - 優點：
    - 高峰時段不會出現「所有人同時按下生成」的尖峰流量
    - 醫師體感為「打開就有草稿」，顯著提升效率

- **不建議：完全自動生成並直接寫入病歷、不需人工確認**
  - 風險：
    - 醫療責任與法律風險極高，與「AI 完全代寫病歷」無異
    - 目前法規與實務仍要求：**醫師對病歷內容負最終責任**，必須審閱與簽章

> ✅ 結論：短期以「手動觸發 + 強制人工審核」為主，中長期再逐步導入「自動預生成草稿」，但**所有階段都維持人工確認與電子簽章流程不變**。

### 核心技術組件

#### 1. LLM 服務（大型語言模型）

**推薦模型**：Google Gemini 3 系列

| 模型 | 上下文窗口 | 特點 | 適用場景 |
|------|-----------|------|---------|
| **gemini-3-flash-preview** | 1M tokens | 高速、高性價比、支援思考模式 | 日常 Progress Note 生成（推薦） |
| **gemini-3-pro-preview** | 1M tokens | 最強推理能力、複雜問題處理 | 複雜病例分析、疑難雜症 |
| GPT-4o | 128K tokens | OpenAI 旗艦模型 | 備用方案 |

**上下文窗口實際容量**：
- 1M tokens ≈ 75-100 萬中文字
- 可容納約 1,500 頁單倍行距文字
- 足以裝入單一病患的完整病歷史

#### 2. 向量資料庫（Vector Database）

**功能**：儲存病人資料的向量嵌入（Embedding），支援語義搜尋

**Embedding 模型選擇**：

| 模型 | 維度 | 提供者 | 說明 |
|------|------|--------|------|
| `text-embedding-3-small` | 1536 維 | OpenAI | 性價比高，推薦 |
| `text-embedding-3-large` | 3072 維 | OpenAI | 精度最高，成本較高 |
| `text-embedding-004` | 768 維 | Google | 免費額度較多 |

> ⚠️ **注意**：選定 Embedding 模型後需保持一致，不同維度的向量無法混用

**資料隔離策略**：
- 每個病人的資料以 `patient_id` 作為命名空間（Namespace）
- 搜尋時必須指定 `patient_id`，確保只搜尋該病人的資料
- 資料庫層級實作 Row-Level Security (RLS)

**技術選項**：
- **方案 A**：Supabase + pgvector（推薦）
  - 優點：與現有系統整合容易、PostgreSQL 生態系成熟
  - 缺點：需要自行維護
- **方案 B**：Pinecone（雲端服務）
  - 優點：免維護、高效能、自動擴展
  - 缺點：成本較高、資料在雲端
  - **備註**：有免費方案（1 個 project、100K 向量），適合 POC
- **方案 C**：Qdrant（開源，可自架）
  - 優點：開源免費、可本地部署
  - 缺點：需要自行維護與擴展

#### 3. 資料同步層（ETL Pipeline）

**功能**：定期從 HIS 系統同步資料，轉換為向量並儲存

**處理流程**：
```
HIS 系統資料
    ↓
資料提取（API 或資料庫連線）
    ↓
資料清理與標準化
    ↓
醫療文件智慧分段（Semantic Chunking）
    ↓
向量化（Embedding）- 使用 AI 模型轉換為向量
    ↓
儲存至向量資料庫（帶 patient_id 標籤）
```

**醫療文件分段策略（優化版）**：

> ⚠️ **重要**：醫療文件有其特殊性，不應使用固定長度分段

| 文件類型 | 分段策略 | 說明 |
|---------|---------|------|
| **檢驗報告** | 完整保留 | 一份報告作為一個 chunk，不可切分 |
| **影像報告** | 完整保留 | 一份報告作為一個 chunk |
| **病歷記錄** | 按就診日期分段 | 每次就診為一個 chunk |
| **護理記錄** | 按班次分段 | 每班次 8 小時為一個 chunk |
| **用藥記錄** | 結構化保留 | 整份用藥清單為一個 chunk |
| **過敏史** | 完整保留 | 短文件，完整保留 |
| **手術記錄** | 完整保留 | 一次手術為一個 chunk |

**關鍵設計**：
- **增量同步**：只同步新增或更新的資料，避免重複處理
- **語義分段**：根據文件類型採用不同分段策略，保持資訊完整性
- **元資料標記**：每個片段標記來源（檢查報告、病歷、用藥記錄等）、日期、類型

---

## 💡 資料隔離解決方案

### 問題：所有病人資料會混在一起嗎？

**答案：不會！** 透過以下技術確保資料完全隔離：

### 方案一：命名空間隔離（Namespace Isolation）

**實作方式**：
```typescript
// 每個病人的資料都有獨立的命名空間
const patientNamespace = `patient_${patientId}`;

// 儲存時
await vectorDB.upsert({
    id: `patient_${patientId}_chunk_001`,
    namespace: patientNamespace,  // 關鍵：使用 patient_id 作為命名空間
    values: embedding,
    metadata: {
        patient_id: patientId,
        source_type: 'lab_report',
        date: '2026-01-15'
    }
});

// 搜尋時（必須指定 patient_id）
const results = await vectorDB.query({
    namespace: patientNamespace,  // 只搜尋該病人的資料
    vector: queryEmbedding,
    topK: 10
});
```

**優點**：
- 資料完全隔離，技術上不可能混亂
- 搜尋效能高（只搜尋該病人的資料）
- 符合資料隔離合規要求

### 方案二：資料庫層級隔離（Database-Level Isolation）

**實作方式**：
```sql
-- 在 PostgreSQL 中使用 Row-Level Security (RLS)
CREATE TABLE patient_embeddings (
    id UUID PRIMARY KEY,
    patient_id VARCHAR(255) NOT NULL,
    content_embedding vector(1536),  -- 使用 text-embedding-3-small
    source_type VARCHAR(50),
    document_date DATE,
    created_at TIMESTAMPTZ
);

-- 啟用 RLS
ALTER TABLE patient_embeddings ENABLE ROW LEVEL SECURITY;

-- 建立政策：只能存取指定的病人資料
CREATE POLICY "Isolate patient data"
    ON patient_embeddings
    FOR ALL
    USING (
        patient_id = current_setting('app.current_patient_id')::VARCHAR
    );
```

**優點**：
- 資料庫層級強制隔離
- 即使程式碼錯誤也不會洩漏資料
- 符合醫療法規要求

### 方案三：混合方案（推薦）

**結合命名空間 + RLS**：
- 應用層：使用命名空間隔離（效能好）
- 資料庫層：使用 RLS 作為第二道防線（安全性高）

---

## 🚀 執行方案與成本評估

### 方案零：快速驗證方案（Phase 0，強烈推薦先執行）

> 💡 **新增建議**：在投入大量資源建置向量資料庫之前，先用最簡單的方式驗證 AI 生成品質

#### 架構選擇
- **LLM 服務**：Google Gemini 3 Flash（1M token 上下文）
- **架構**：直接傳入完整病歷，無需向量資料庫
- **整合**：手動準備 5-10 個測試病例

#### 驗證流程
```
1. 從 HIS 系統手動匯出 5-10 個病例資料
2. 整理成文字格式
3. 直接傳入 Gemini 3 Flash API
4. 評估生成的 Progress Note 品質
5. 決定是否繼續投資
```

#### 成本估算

| 項目 | 成本 (TWD) | 說明 |
|------|-----------|------|
| Gemini API 費用 | < $1,500 | 測試用量極低 |
| 人力（資料整理） | $10,000 - $20,000 | 手動整理測試資料 |
| **總計** | **< $25,000** | 2 週內完成 |

#### 優點
- ✅ 成本極低（< $25,000）
- ✅ 速度極快（1-2 週）
- ✅ 無需技術建置
- ✅ 快速驗證 AI 生成品質是否符合需求

#### 缺點
- ❌ 不適合正式上線（無資料隔離機制）
- ❌ 每次都傳送完整資料（成本較高）

#### 適用情境
- **必做**：任何正式投資前的概念驗證
- 釐清「AI 生成品質是否達標」這個關鍵問題

---

### 方案一：雲端服務方案（推薦給快速上線）

#### 架構選擇
- **向量資料庫**：Pinecone（雲端服務）
- **LLM 服務**：Google Gemini 3 Flash
- **應用伺服器**：Google Cloud Run / AWS Lambda（Serverless）
- **資料庫**：Supabase（PostgreSQL）

#### 成本估算（月費）

| 項目 | 規格 | 月費 (USD) | 月費 (TWD) | 說明 |
|------|------|-----------|-----------|------|
| Pinecone | Free → Standard | $0 - $70 | $0 - $2,100 | 免費方案可用於 POC，正式上線用 Standard |
| Gemini API | 預估 10K 次請求 | $30 - $50 | $900 - $1,500 | Gemini 3 Flash 定價較低 |
| Supabase | Pro 方案 | $25 | $750 | 資料庫與 API |
| Cloud Run | 預估用量 | $30 | $900 | Serverless 運算 |
| **總計** | - | **$85 - $175** | **$2,550 - $5,250** | 不含一次性開發成本 |

#### 優點
- ✅ 快速上線（1-2 個月）
- ✅ 免維護，自動擴展
- ✅ 初期成本可控（使用免費方案）
- ✅ 效能穩定

#### 缺點
- ❌ 資料在雲端（需確認法規允許）
- ❌ 長期成本可能上升

#### 適用情境
- 希望快速驗證概念（MVP）
- 沒有 IT 維護團隊
- 重視時間成本

---

### 方案二：混合雲方案（推薦給平衡成本與控制）

#### 架構選擇
- **向量資料庫**：Supabase + pgvector（自架向量搜尋）
- **LLM 服務**：Google Gemini 3 Flash
- **應用伺服器**：自架伺服器（或 Google Cloud VM）
- **資料庫**：Supabase（PostgreSQL）

#### 成本估算（月費）

| 項目 | 規格 | 月費 (USD) | 月費 (TWD) | 說明 |
|------|------|-----------|-----------|------|
| Supabase | Pro → Team | $25 - $599 | $750 - $18,000 | 依資料量選擇方案 |
| Gemini API | 預估 10K 次請求 | $30 - $50 | $900 - $1,500 | 依實際用量計費 |
| 伺服器 | Google Cloud VM (4 vCPU, 16GB) | $150 | $4,500 | 應用伺服器 |
| 備份與監控 | - | $50 | $1,500 | 備份與監控服務 |
| **總計** | - | **$255 - $849** | **$7,650 - $25,500** | 不含一次性開發成本 |

#### 優點
- ✅ 資料控制權在自己手上
- ✅ 可客製化程度高
- ✅ 成本彈性大（可從小規模開始）

#### 缺點
- ❌ 需要 IT 維護能力
- ❌ 上線時間較長（3-4 個月）

#### 適用情境
- 有基本 IT 維護能力
- 重視資料主權
- 希望長期成本可控

---

### 方案三：完全自架方案（推薦給大型醫院）

#### 架構選擇
- **向量資料庫**：Qdrant（開源，自架）
- **LLM 服務**：Google Gemini 3 Flash（或自架開源模型）
- **應用伺服器**：自架伺服器集群
- **資料庫**：PostgreSQL + pgvector（自架）

#### 成本估算（月費）

| 項目 | 規格 | 月費 (USD) | 月費 (TWD) | 說明 |
|------|------|-----------|-----------|------|
| 伺服器 | 3 台 VM (高可用) | $300 | $9,000 | 應用 + 資料庫 + 向量庫 |
| Gemini API | 預估 10K 次請求 | $30 - $50 | $900 - $1,500 | 或使用開源模型 |
| 儲存空間 | 500GB SSD | $50 | $1,500 | 資料儲存 |
| 網路與 CDN | - | $30 | $900 | 網路頻寬 |
| 維護人力 | 1 名工程師（部分時間） | $1,500 - $2,500 | $45,000 - $75,000 | 系統維護（修正：台灣工程師合理薪資） |
| **總計** | - | **$1,910 - $2,930** | **$57,300 - $87,900** | 不含一次性開發成本 |

#### 優點
- ✅ 完全控制資料與系統
- ✅ 可深度客製化
- ✅ 符合最嚴格的法規要求

#### 缺點
- ❌ 需要專業 IT 團隊
- ❌ 上線時間最長（6 個月以上）
- ❌ 維護成本高

#### 適用情境
- 大型醫院（500+ 床）
- 有完整 IT 部門
- 重視資料隱私與主權

---

## 📊 方案比較總表

| 比較項目 | Phase 0：快速驗證 | 方案一：雲端服務 | 方案二：混合雲 | 方案三：完全自架 |
|---------|-----------------|----------------|--------------|----------------|
| **一次性成本** | < $25,000 | $1,150,000 - $2,300,000 | $1,150,000 - $2,300,000 | $1,500,000 - $3,000,000 |
| **月費成本** | N/A | $2,550 - $5,250 | $7,650 - $25,500 | $57,300 - $87,900 |
| **上線時間** | 1-2 週 | 1-2 個月 | 3-4 個月 | 6 個月以上 |
| **維護需求** | 無 | 低 | 中 | 高 |
| **資料控制** | 手動 | 雲端 | 混合 | 完全自主 |
| **適用目的** | 概念驗證 | MVP/小規模 | 正式上線 | 大規模部署 |
| **技術門檻** | 極低 | 低 | 中 | 高 |

---

## 🔧 技術實作細節

### 1. 資料同步流程（ETL Pipeline）

```typescript
// lib/medical/etl-pipeline.ts

/**
 * 從 HIS 系統同步病人資料並轉換為向量
 */
export async function syncPatientData(patientId: string) {
    // Step 1: 從 HIS 系統取得資料
    const hisData = await fetchFromHIS({
        patientId,
        dataTypes: ['lab_reports', 'medical_records', 'medications', 'allergies']
    });

    // Step 2: 資料清理與標準化
    const cleanedData = cleanAndNormalize(hisData);

    // Step 3: 醫療文件智慧分段（Semantic Chunking）
    const chunks = semanticChunkMedicalDocuments(cleanedData);
    // 註：使用語義分段而非固定長度分段

    // Step 4: 為每個片段生成向量
    const embeddings = await Promise.all(
        chunks.map(chunk => generateEmbedding(chunk.text))
    );

    // Step 5: 儲存至向量資料庫（帶 patient_id 標籤）
    await vectorDB.upsert(
        chunks.map((chunk, index) => ({
            id: `patient_${patientId}_${chunk.sourceType}_${chunk.date}`,
            namespace: `patient_${patientId}`,  // 關鍵：命名空間隔離
            values: embeddings[index],
            metadata: {
                patient_id: patientId,
                source_type: chunk.sourceType,
                document_date: chunk.date,
                chunk_index: index
            }
        }))
    );
}

/**
 * 醫療文件智慧分段
 */
function semanticChunkMedicalDocuments(data: MedicalData): Chunk[] {
    const chunks: Chunk[] = [];

    // 檢驗報告：完整保留
    for (const report of data.labReports) {
        chunks.push({
            text: formatLabReport(report),
            sourceType: 'lab_report',
            date: report.reportDate
        });
    }

    // 病歷記錄：按就診日期分段
    for (const record of data.medicalRecords) {
        chunks.push({
            text: formatMedicalRecord(record),
            sourceType: 'medical_record',
            date: record.visitDate
        });
    }

    // 用藥記錄：結構化保留
    chunks.push({
        text: formatMedicationList(data.medications),
        sourceType: 'medications',
        date: new Date().toISOString()
    });

    // 過敏史：完整保留
    if (data.allergies.length > 0) {
        chunks.push({
            text: formatAllergies(data.allergies),
            sourceType: 'allergies',
            date: new Date().toISOString()
        });
    }

    return chunks;
}
```

### 2. Progress Note 生成流程

```typescript
// lib/medical/progress-note-generator.ts

/**
 * 生成 Progress Note
 */
export async function generateProgressNote(
    patientId: string,
    visitDate: string,
    physicianId: string
) {
    // Step 1: 將查詢轉換為向量
    const query = "生成 Progress Note，包含主訴、現病史、理學檢查、診斷、處置計畫";
    const queryEmbedding = await generateEmbedding(query);

    // Step 2: 在向量資料庫中搜尋（限定該病人）
    const relevantChunks = await vectorDB.query({
        namespace: `patient_${patientId}`,  // 只搜尋該病人的資料
        vector: queryEmbedding,
        topK: 15,  // 取前 15 個相關片段
        filter: {
            document_date: { $gte: getRecentDateRange(visitDate, 90) }  // 最近 90 天
        }
    });

    // Step 3: 組合成上下文
    const context = formatContext(relevantChunks, {
        includeLabReports: true,
        includeMedications: true,
        includeAllergies: true,
        includeRecentVisits: true
    });

    // Step 4: 呼叫 LLM 生成 Progress Note
    const progressNote = await callLLM({
        model: 'gemini-3-flash-preview',  // 使用 Gemini 3 Flash
        prompt: buildProgressNotePrompt(context, visitDate),
        temperature: 0.3,  // 低溫度確保一致性
        maxTokens: 2000
    });

    // Step 5: 格式驗證
    const validated = validateProgressNoteFormat(progressNote);

    // Step 6: 返回結果（需醫生審核）
    return {
        content: validated.content,
        sources: validated.sources,  // 引用來源
        confidence: validated.confidence,
        requiresReview: true,  // 必須審核
        generatedAt: new Date().toISOString(),
        aiModel: 'gemini-3-flash-preview'  // 記錄使用的模型
    };
}
```

### 3. 住院摘要生成流程

```typescript
// lib/medical/hospital-summary-generator.ts

/**
 * 生成住院摘要
 */
export async function generateHospitalSummary(
    patientId: string,
    admissionDate: string,
    dischargeDate: string
) {
    // Step 1: 搜尋住院期間的所有資料
    const queryEmbedding = await generateEmbedding(
        "生成住院摘要，包含入院原因、住院期間重要事件、目前狀況、用藥調整"
    );

    // Step 2: 搜尋住院期間的資料（時間範圍過濾）
    const relevantChunks = await vectorDB.query({
        namespace: `patient_${patientId}`,
        vector: queryEmbedding,
        topK: 20,
        filter: {
            document_date: {
                $gte: admissionDate,
                $lte: dischargeDate
            },
            source_type: {
                $in: ['nursing_notes', 'vital_signs', 'lab_reports', 'medications']
            }
        }
    });

    // Step 3: 按時間排序
    const sortedChunks = sortByDate(relevantChunks);

    // Step 4: 生成時間軸摘要
    const summary = await callLLM({
        model: 'gemini-3-flash-preview',
        prompt: buildHospitalSummaryPrompt(sortedChunks, {
            admissionDate,
            dischargeDate
        }),
        temperature: 0.3
    });

    return {
        content: summary,
        timeline: extractTimeline(summary),
        keyEvents: extractKeyEvents(summary),
        medications: extractMedicationChanges(summary),
        requiresReview: true,
        generatedAt: new Date().toISOString()
    };
}
```

---

## 🔐 安全性與合規性

### 資料加密

- **傳輸加密**：TLS 1.3
- **儲存加密**：AES-256（資料庫層級）
- **向量資料加密**：向量本身不包含敏感資訊，但元資料需加密

### 存取控制

- **身份驗證**：與 HIS 系統整合 SSO
- **權限控制**：Role-Based Access Control (RBAC)
- **資料隔離**：命名空間 + RLS 雙重保護

### 稽核記錄

- **完整記錄**：所有資料存取都記錄 Audit Log
- **保留期限**：依醫療法規保存 7 年
- **不可篡改**：使用數位簽章確保完整性
- **版本追蹤**：記錄 AI 原始輸出 vs 醫師修改版本

### 台灣醫療法規合規

- **個人資料保護法**：病患資料加密儲存、存取控制、稽核記錄
- **醫療法第 72 條**：病歷（含 AI 生成內容）保存至少 7 年
- **電子病歷管理辦法**：電子簽章、版本控制、不可篡改

---

## 📅 實施時程規劃

### 階段零：快速驗證（Phase 0）- 2 週

**目標**：驗證 AI 生成品質是否符合需求

- Week 1：準備測試資料（5-10 個病例）
- Week 2：使用 Gemini 3 Flash 測試生成品質

**交付物**：
- AI 生成品質評估報告
- Go/No-Go 決策

**決策點**：
- ✅ 品質達標 → 進入階段一
- ❌ 品質不達標 → 調整 Prompt 或評估其他模型

### 階段一：概念驗證（POC）- 1 個月

**目標**：驗證完整技術架構可行性

- Week 1-2：設定向量資料庫環境、HIS 資料格式分析
- Week 3：實作資料同步流程（1-2 個病人）
- Week 4：實作 Progress Note 生成（含審核流程）

**交付物**：
- POC 系統
- HIS 整合評估報告
- 技術驗證報告

### 階段二：MVP 開發 - 2-3 個月

**目標**：開發最小可行產品

- Month 1：完整 ETL Pipeline 開發 + HIS 整合
- Month 2：Progress Note 生成功能開發（含審核與簽章）
- Month 3：住院摘要生成功能開發 + 測試

**交付物**：
- MVP 系統（可正式使用）
- 使用者手冊
- 測試報告
- 資安審核文件

### 階段三：試點上線 - 1-2 個月

**目標**：小規模試點，收集回饋

- Week 1-2：選擇 1-2 個科別試點
- Week 3-4：收集使用者回饋
- Week 5-6：優化與調整
- Week 7-8：擴大試點範圍

**交付物**：
- 試點報告
- 使用者回饋分析
- 優化建議

### 階段四：全院推廣 - 2-3 個月

**目標**：全院上線

- Month 1：逐步開放各科別
- Month 2：持續監控與優化
- Month 3：正式上線，提供支援

**總時程**：6-9 個月（含 Phase 0）

---

## 💰 總成本估算

### 一次性成本（開發）

| 項目 | 成本 (TWD) | 說明 |
|------|-----------|------|
| Phase 0 快速驗證 | $25,000 | 建議必做 |
| 系統開發 | $800,000 - $1,500,000 | 依方案複雜度 |
| HIS 系統整合 | $150,000 - $700,000 | **修正**：含 API 授權、資料轉換、資安審核 |
| 測試與驗證 | $100,000 - $200,000 | 功能測試、壓力測試 |
| 教育訓練 | $50,000 - $100,000 | 使用者訓練 |
| 法規顧問 | $50,000 - $100,000 | **新增**：醫療法規合規諮詢 |
| **總計** | **$1,175,000 - $2,625,000** | 一次性投資 |

### 持續性成本（月費）

| 方案 | 月費 (TWD) | 年費 (TWD) |
|------|-----------|-----------|
| 方案一：雲端服務 | $2,550 - $5,250 | $30,600 - $63,000 |
| 方案二：混合雲 | $7,650 - $25,500 | $91,800 - $306,000 |
| 方案三：完全自架 | $57,300 - $87,900 | $687,600 - $1,054,800 |

### 3 年總成本估算

| 方案 | 開發成本 | 3 年月費 | **總計** |
|------|---------|---------|---------|
| 方案一 | $1,175,000 - $2,625,000 | $91,800 - $189,000 | **$1,266,800 - $2,814,000** |
| 方案二 | $1,175,000 - $2,625,000 | $275,400 - $918,000 | **$1,450,400 - $3,543,000** |
| 方案三 | $1,175,000 - $2,625,000 | $2,062,800 - $3,164,400 | **$3,237,800 - $5,789,400** |

---

## 🎯 建議方案

### 針對苗栗大千醫院的建議

根據您的描述（中型醫院、希望快速解決問題），我建議採用以下分階段策略：

#### 推薦路徑

```
Phase 0（必做）          Phase 1               Phase 2
快速驗證 ───────────────▶ 雲端服務方案 ──────────▶ 混合雲方案
2 週、$25K              2-3 個月、MVP          3-4 個月、正式上線
```

#### 理由：

1. **Phase 0 快速驗證是必做的**：
   - 成本極低（< $25,000），但可以釐清最關鍵的問題：AI 生成品質
   - 避免投入大量資源後發現品質不符需求

2. **先用雲端服務快速上線**：
   - 使用 Pinecone 免費方案 + Gemini 3 Flash，成本可控
   - 快速建立 MVP，收集真實使用回饋

3. **成熟後遷移至混合雲**：
   - 資料控制權在自己手上
   - 長期成本可控
   - 符合醫療法規要求

#### 實施建議：

1. **立即執行 Phase 0**（本週開始）
2. **Phase 0 成功後**：啟動 HIS 廠商溝通（這是最長的前置作業）
3. **Phase 1**：採用雲端服務方案快速驗證
4. **Phase 2**：遷移至混合雲方案，建立長期架構

---

## ⚠️ 風險評估與緩解策略

| 風險 | 可能性 | 影響 | 緩解策略 |
|------|--------|------|---------|
| **AI 生成品質不符需求** | 中 | 極高 | **Phase 0 快速驗證**，早期發現問題 |
| **HIS 系統整合困難** | 高 | 高 | 早期與 HIS 廠商溝通、預留整合時間、備妥替代方案（手動匯入） |
| **資料外洩** | 低 | 極高 | 多層加密 + RLS + 定期安全審計 |
| **AI 生成錯誤導致醫療疏失** | 中 | 極高 | 強制人工審核 + 電子簽章 + 版本記錄 + 責任歸屬明確 |
| **系統當機** | 低 | 高 | 高可用性架構 + 災難復原計畫 |
| **成本超支** | 中 | 中 | 設定預算上限 + 用量監控 + 分階段投資 |
| **使用者抗拒** | 中 | 中 | 充分教育訓練 + 展示效益 + 早期試點收集回饋 |
| **法規合規問題** | 中 | 高 | 聘請醫療法規顧問、電子簽章機制、完整稽核記錄 |

---

## 📚 技術參考資料

### 向量資料庫文件

- [Supabase pgvector 文件](https://supabase.com/docs/guides/ai/vector-columns)
- [Pinecone 文件](https://docs.pinecone.io/)
- [Qdrant 文件](https://qdrant.tech/documentation/)

### LLM API 文件

- [Google Gemini 3 API](https://ai.google.dev/gemini-api/docs/gemini-3)
- [Gemini 3 Flash 文件](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/models/gemini/3-flash)
- [Gemini 3 Pro 文件](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/models/gemini/3-pro)
- [OpenAI API](https://platform.openai.com/docs)

### 醫療資料標準

- [HL7 FHIR](https://www.hl7.org/fhir/)
- [ICD-10 診斷編碼](https://www.who.int/standards/classifications/classification-of-diseases)

### 台灣醫療法規

- [個人資料保護法](https://law.moj.gov.tw/LawClass/LawAll.aspx?pcode=I0050021)
- [醫療法](https://law.moj.gov.tw/LawClass/LawAll.aspx?pcode=L0020021)
- [電子病歷管理辦法](https://law.moj.gov.tw/LawClass/LawAll.aspx?pcode=L0020121)

---

## 📝 結論

### 核心建議

1. **先做 Phase 0 快速驗證**：用最低成本驗證 AI 生成品質
2. **HIS 整合是最大挑戰**：早期與 HIS 廠商溝通，預留足夠時間與預算
3. **法規合規不可忽視**：AI 生成內容仍需醫師審核並簽章
4. **分階段實施**：Phase 0 → 雲端服務 MVP → 混合雲正式上線
5. **資料隔離不是問題**：透過命名空間 + RLS 可確保完全隔離

### 下一步行動

1. **立即**：啟動 Phase 0 快速驗證（本週）
2. **Phase 0 期間**：與 HIS 廠商溝通 API 可用性與費用
3. **Phase 0 成功後**：確認預算範圍，啟動 Phase 1 開發
4. **同步進行**：諮詢醫療法規顧問，確保合規

### 預算建議

| 階段 | 預算 (TWD) | 說明 |
|------|-----------|------|
| Phase 0 | $25,000 | 快速驗證，必做 |
| Phase 1 MVP | $800,000 - $1,500,000 | 含 HIS 整合 |
| Phase 2 正式上線 | $200,000 - $500,000 | 優化與擴展 |
| 月費（混合雲） | $7,650 - $25,500 | 依規模調整 |

---

**文件版本**：v2.0
**最後更新**：2026-01-20
**主要更新內容**：
- 新增 Phase 0 快速驗證方案（使用 Gemini 3 長上下文）
- 更新 LLM 模型為 Gemini 3 Flash/Pro（1M token 上下文）
- 新增台灣醫療法規合規章節
- 新增 AI 生成內容法律責任說明
- 補充 HIS 系統整合挑戰與成本估算
- 優化醫療文件分段策略（語義分段）
- 修正成本估算（更貼近實際）
- 新增向量維度與 Embedding 模型說明

**參考資料來源**：
- [Gemini 3 Flash Preview - OpenRouter](https://openrouter.ai/google/gemini-3-flash-preview)
- [Gemini 3 Pro - Google Cloud](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/models/gemini/3-pro)
- [Gemini 3 Context Window - Thinkpeak AI](https://thinkpeak.ai/gemini-3-context-window-size-1-2m-tokens/)

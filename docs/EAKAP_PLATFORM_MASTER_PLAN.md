# 🚀 EAKAP 企業 AI 平台 — 完整戰略企劃書

**文件版本：** 2.0（整合版）  
**建立日期：** 2026-01-12  
**核心定位：** 企業 AI 的 iOS — 讓所有 AI 能力觸手可及  
**設計哲學：** 賈伯斯式的極簡主義 + 零學習曲線 + 主動式 AI 體驗

---

## 📋 目錄

1. [執行摘要](#執行摘要)
2. [戰略定位與願景](#戰略定位與願景)
3. [架構設計](#架構設計)
4. [產品設計](#產品設計)
5. [主動式 AI 體驗](#主動式-ai-體驗)
6. [技術設計](#技術設計)
7. [安全性設計](#安全性設計)
8. [商業模式](#商業模式)
9. [實施路線圖](#實施路線圖)
10. [風險評估與緩解](#風險評估與緩解)
11. [成功指標與願景](#成功指標與願景)

---

## 執行摘要

### 核心問題

目前 EAKAP 知識架構師是一個單體應用，功能越加越多，面臨以下挑戰：
- 客戶可能只需要部分功能，卻要付全價
- 難以擴展新的收入來源
- 與競爭對手進入「功能戰」
- AI 停留在「被動回應」模式，使用者需主動提問

### 解決方案

將系統重構為**「AI 大腦平台 + Agent 應用生態」**模式：

```
┌─────────────────────────────────────────────────────────────────┐
│                    EAKAP 企業 AI 平台架構                         │
├─────────────────────────────────────────────────────────────────┤
│  Layer 0: 資料主權層                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                          │
│  │ S3/MinIO │  │ Supabase │  │ 客戶自有 │                          │
│  │ 檔案儲存 │  │ 資料庫   │  │ 資料庫   │                          │
│  └─────────┘  └─────────┘  └─────────┘                          │
├─────────────────────────────────────────────────────────────────┤
│  Layer 1: AI 大腦層（核心平台）                                   │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐            │
│  │ 知識攝取 │  │ 向量搜尋 │  │ AI 引擎 │  │ 主動偵測 │            │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘            │
│                         ▼ 公開 REST API                          │
├─────────────────────────────────────────────────────────────────┤
│  Layer 2: Agent 應用層（獨立部署，可分開銷售）                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ 戰情決策  │  │ 賈維斯   │  │ Line Bot │  │ 智能客服  │        │
│  │ Dashboard │  │ 語音助理 │  │ 助理     │  │ Bot      │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

### 核心價值主張

| iOS 之於 iPhone | Agent Hub 之於企業 AI |
|----------------|----------------------|
| 讓使用者輕鬆使用 App | 讓企業輕鬆使用 AI Agent |
| App Store 生態系統 | Agent Store 生態系統 |
| 簡單、直覺、美觀 | 零學習曲線、一個介面 |
| 統一的開發規範 | 統一的 Agent Protocol |
| 硬體 + 軟體 + 服務 | 平台 + Agent + 企業服務 |

---

## 戰略定位與願景

### 現狀分析

```
知識架構師 (eakap.com) — 目前
└─ 企業內部使用（單體應用）
   ├─ 知識庫管理
   ├─ Agent 工廠（自建 Agent）
   ├─ 戰情室儀表板
   └─ 被動式 AI 對話
```

### 目標架構

```
EAKAP Agent Hub (hub.eakap.com) — 未來
└─ 面向終端使用者的「AI 工作台」
   ├─ 使用自家企業的 Agent（透過 API 連接）
   ├─ 下載/使用第三方 Agent
   ├─ 整合 MCP Servers（Gmail、Slack、Notion）
   ├─ 整合 Claude Skills
   ├─ 統一的對話介面（極致簡化）
   └─ 主動式 AI 體驗（賈維斯級）
```

### 產業趨勢驗證

全球一線 AI 廠商都在做「平台 + 生態」：

| 公司 | 策略 | 啟示 |
|-----|------|------|
| Anthropic | 大腦 + Skills | 核心模型 + 可插拔技能 |
| Microsoft | 知識圖譜 + Copilot | 企業知識 + 多元應用 |
| OpenAI | GPT + Store | 基礎模型 + 開放生態 |
| Salesforce | Data Cloud + Agentforce | 客戶資料 + AI Agent |

**結論：您的直覺是正確的，平台模式是產業趨勢。**

### 台灣中小企業特殊需求

| 需求 | 說明 | 我們的優勢 |
|-----|------|-----------|
| 在地化 | 繁體中文、台灣法規 | ✅ 已有 |
| 簡單易用 | SMB 沒有工程師 | ✅ 可視化 Agent 工廠 |
| 彈性定價 | 預算有限，需分開買 | ✅ 平台模式可滿足 |
| 資料落地 | 擔心資料外洩 | ⚠️ 可考慮私有部署選項 |

---

## 架構設計

### 三層分離模型

| 層級 | 名稱 | 職責 | 商業定位 |
|-----|------|------|---------|
| Layer 0 | 資料主權層 | 儲存原始檔案、資料庫 | 客戶可選擇自建或託管 |
| Layer 1 | AI 大腦層 | 知識處理、Agent 運算、API 服務 | **核心產品，長期訂閱** |
| Layer 2 | Agent 應用層 | 終端使用者介面、場景解決方案 | **模組化產品，按需購買** |

### 現有系統技術基礎評估

好消息是，您的系統已經有良好的分層基礎：

```
目前的程式碼結構
├── lib/
│   ├── knowledge/       ← 知識處理核心 ✅ 可抽離
│   ├── gemini/          ← AI 引擎核心 ✅ 可抽離
│   ├── war-room/        ← 戰情室邏輯 ⚠️ 與知識庫耦合
│   └── supabase/        ← 資料層 ✅ 可抽離
├── app/api/
│   ├── agents/          ← Agent API ✅ 已是 API 形式
│   ├── chat/            ← 對話 API ✅ 已是 API 形式
│   ├── files/           ← 檔案 API ✅ 已是 API 形式
│   └── knowledge/       ← 知識 API ✅ 已是 API 形式
```

**結論：技術上可行，且成本可控。**

### 核心 API 設計

```typescript
// 「AI 大腦」對外開放的核心 API

// Knowledge API - 知識管理
'/api/v1/documents': {
  POST: (file, metadata) => Document;
  GET: (query) => Document[];
};

'/api/v1/search': {
  POST: (query, options) => SearchResult[];
};

'/api/v1/frameworks': {
  GET: (documentId) => ExtractedFrameworks;
  POST: (documentId, frameworkType) => Framework;
};

// Agent API - Agent 運算
'/api/v1/agents': {
  POST: (config) => Agent;
  GET: () => Agent[];
};

'/api/v1/agents/:id/chat': {
  POST: (message, context) => AgentResponse;
};

// Intelligence API - 智能分析
'/api/v1/intelligence/risks': {
  GET: (scope) => RiskAlert[];
};

'/api/v1/intelligence/insights': {
  POST: (context) => Insight[];
};
```

### API 版本管理策略

```
URL 結構：
https://brain.eakap.com/api/v1/...

版本演進：
- v1: 當前穩定版本，承諾向後相容 2 年
- v2: 下一代 API，可能有破壞性變更
- beta: 實驗性功能，不保證穩定

Header 認證：
Authorization: Bearer <API_KEY>
X-Organization-ID: <ORG_ID>
```

---

## 產品設計

### 設計哲學：賈伯斯式的極簡主義

#### 1. Zero Learning Curve（零學習曲線）
- 使用者打開就會用，不需要看教學
- 所有複雜的技術細節隱藏在背後
- 首頁只有一個對話框 + 3 個推薦的常用 Agent

#### 2. One Interface, All AI（一個介面，所有 AI）
- 不管是自家 Agent、第三方 Agent、MCP、Skills，都在同一個對話框操作
- 使用者不需要知道「這是 MCP」或「這是 Agent」
- Master Agent 自動路由到正確的能力

#### 3. Progressive Disclosure（漸進式揭露）
- 初次使用：只看到對話框 + 3 個常用 Agent
- 進階使用：才看到「Agent 商店」、「我的 Agent」、「設定」

#### 4. Beautiful by Default（預設就美觀）
- 不需要使用者自己調整 UI，開箱即美
- 黑暗模式優先（適合專業使用者）

### 主介面設計

#### 首頁：極簡對話框

```
┌─────────────────────────────────────────────────────────────┐
│                         EAKAP Agent Hub                       │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  💬 您想做什麼？                                         ││
│  │  [                                                       ]││
│  │                                                       🎤 ││
│  └─────────────────────────────────────────────────────────┘│
│                                                               │
│  💡 常用 Agent                                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ 📊 財報   │  │ 📧 客服   │  │ 🎯 專案   │                  │
│  │ 分析師   │  │ 小幫手   │  │ 管理      │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
│                                                               │
│  🌐 最近使用                                                  │
│  • 分析 Q1 營收趨勢（2 小時前）                               │
│  • 回覆客戶詢問信件（昨天）                                   │
└─────────────────────────────────────────────────────────────┘
```

**設計要點**：
- 全螢幕中央對話框：類似 Spotlight（macOS）或 Raycast
- 快捷鍵啟動：⌘ + K（macOS）/ Ctrl + K（Windows）
- 語音輸入：點擊麥克風圖示即可語音輸入

#### 對話進行中

```
┌─────────────────────────────────────────────────────────────┐
│  [←]  對話                                          [⚙️] [✕]  │
├─────────────────────────────────────────────────────────────┤
│  👤 您：幫我分析 Q1 財報並寄給 CEO                            │
│  ─────────────────────────────────────────────────────────  │
│  🤖 Agent Hub                                               │
│  好的，我正在處理：                                          │
│  ✅ 1. 讀取 Q1 財報文件                                      │
│  🔄 2. 分析財務數據...                                       │
│  ⏸️ 3. 準備寄送給 CEO                                        │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📊 Q1 財報分析摘要                                    │   │
│  │ • 營收：$1.2M（+15% YoY）                             │   │
│  │ • 毛利率：42%（-2% QoQ）                              │   │
│  │ • 現金流：$300K（健康）                               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  是否要立即寄送給 CEO？                                      │
│  [📧 立即寄送] [✏️ 修改內容] [❌ 取消]                        │
│  ─────────────────────────────────────────────────────────  │
│  💡 使用了：財報分析 Agent、Gmail MCP                        │
└─────────────────────────────────────────────────────────────┘
```

### 色彩系統

```typescript
export const AGENT_HUB_THEME = {
    background: {
        primary: '#0A0E27',      // 深藍黑（主背景）
        secondary: '#12182E',    // 次要背景（卡片）
        tertiary: '#1A2238'      // 第三層背景（浮層）
    },
    accent: {
        primary: '#00D9FF',      // 電光藍（重要數據、連結）
        secondary: '#A78BFA'     // 紫光（AI 相關、Magic）
    },
    semantic: {
        success: '#00FF88',      // 翠綠（成功、完成）
        warning: '#FFB800',      // 琥珀黃（警告、待處理）
        danger: '#FF3366',       // 霓虹紅（錯誤、高風險）
        info: '#00D9FF'          // 電光藍（資訊）
    },
    text: {
        primary: '#FFFFFF',      // 主要文字
        secondary: '#B4BCD0',    // 次要文字
        tertiary: '#6B7280'      // 第三層文字
    }
};
```

### 響應式設計

| 螢幕尺寸 | 佈局調整 |
|---------|---------|
| 桌面（> 1024px）| 全功能顯示，側邊欄可選 |
| 平板（768px - 1024px）| 隱藏側邊欄，全螢幕對話框 |
| 手機（< 768px）| 精簡版，底部導航列 |

---

## 主動式 AI 體驗

### 核心理念：從「被動工具」到「主動夥伴」

| 被動模式（現狀） | 主動模式（目標） |
|-----------------|-----------------|
| 使用者問「本週有什麼風險？」| AI 主動說「董事長，我發現一個潛在風險...」|
| 使用者自己看報表 | AI 主動說「您通常關心營收數據，我幫您整理好了」|
| 使用者收到 Email 後進系統 | AI 透過 Line 主動推送「緊急：交期可能延遲」|
| 使用者打字問問題 | 使用者語音說「總結今天重點」，AI 語音回答 |

### Phase A: 智能通知與主動提醒系統

**優先級：** P0（最高）  
**預估工作量：** 2-3 週  
**核心價值：** 從「我要去找資訊」變成「資訊主動來找我」

#### 事件驅動的 AI 洞察

```typescript
interface ProactiveEvent {
  id: string;
  type: 'risk_detected' | 'opportunity_found' | 'deadline_approaching' | 
        'anomaly_detected' | 'document_needs_update' | 'strategic_insight';
  severity: 'info' | 'warning' | 'critical';
  title: string;
  description: string;
  action_required: boolean;
  suggested_actions: string[];
  detected_at: Date;
}
```

#### 每日 AI 簡報

```typescript
async generateDailyBriefing(userId: string): Promise<DailyBriefing> {
  const prompt = `
你是企業決策顧問，請為高階主管製作一份簡潔有力的「每日戰情簡報」。

請用以下格式輸出（使用繁體中文）：

## 🌅 早安，董事長

### 📊 今日重點（30 秒速覽）
- [用一句話總結最重要的 1-2 件事]

### ⚠️ 需要您關注的事項
[列出需要決策或回應的項目，按緊急程度排序]

### 💡 AI 洞察
[提供一個基於數據的戰略性觀點或建議]

### ✅ 建議今日行動
[具體可執行的 2-3 個行動項目]
  `;
  
  return generateContent('gemini-2.0-flash-exp', prompt);
}
```

### Phase B: Line 整合

**優先級：** P0  
**預估工作量：** 3-4 週  
**核心價值：** 讓老闆即使不在辦公室，也能隨時掌握關鍵資訊

#### 為什麼選擇 Line？

| 通訊管道 | 優點 | 適合度 |
|---------|------|--------|
| Line | 台灣滲透率 95%、即時、高開啟率 | ⭐⭐⭐⭐⭐ |
| Email | 正式、可追蹤 | ⭐⭐ |
| Slack | 企業常用、可整合 | ⭐⭐⭐ |

#### 通知觸發時機設計

```typescript
const NOTIFICATION_TRIGGERS = {
  // 即時觸發（發生時立即通知）
  immediate: [
    { event: 'critical_risk_detected', channels: ['line', 'push', 'email'] },
    { event: 'urgent_deadline', channels: ['line', 'push'] },
  ],

  // 批次觸發（每日定時）
  scheduled: [
    { name: 'morning_briefing', cron: '0 7 * * *', channels: ['line'] },
    { name: 'weekly_strategy_report', cron: '0 9 * * 1', channels: ['email', 'line'] },
  ],

  // 行為觸發（基於用戶行為模式）
  behavioral: [
    { name: 'login_greeting', trigger: 'user_login', action: 'show_contextual_summary' },
  ],
};
```

### Phase C: 語音互動系統

**優先級：** P1  
**預估工作量：** 4-6 週  
**核心價值：** 讓老闆邊開車、邊走路也能與系統互動

#### 成本分析

| 互動類型 | 成本估算 | 優化策略 |
|---------|---------|---------|
| 語音轉文字 (STT) | Whisper: $0.006/min | Web Speech API（免費）|
| 文字轉語音 (TTS) | $0.015/1K chars | Edge TTS（免費）|
| AI 回應生成 | $0.0001-$0.0004 | 精簡 Prompt、快取常見問答 |

**結論：使用免費方案可大幅降低成本。**

#### 語音專用 Prompt

```typescript
const VOICE_SYSTEM_PROMPT = `
你是「企業戰情 AI 助理」，正在透過「語音對話」與使用者互動。

## 語音互動準則
1. **簡潔有力**：每次回答控制在 3-4 句話內
2. **口語化**：不要說「本系統偵測到」，要說「我發現」
3. **結構清晰**：用「第一」「第二」明確分隔
4. **確認理解**：不確定時主動確認

## 好的回答範例
「董事長早安！今天有兩件事需要您注意。第一，供應商 A 的交期可能延遲三天，
建議您今天和採購確認。第二，上週的營收比目標高出 8%，是個好消息。」
`;
```

### Phase D: 情境感知與預測性 AI

**優先級：** P0  
**預估工作量：** 3-4 週  
**核心價值：** AI 學習你的習慣，在你開口之前就準備好你需要的東西

#### 情境感知系統

```typescript
interface UserContext {
  time: {
    hour: number;
    dayOfWeek: number;
    isWorkingHours: boolean;
    isMonthEnd: boolean;
  };
  behavior: {
    lastLoginAt: Date;
    frequentlyViewedReports: string[];
    preferredWorkingHours: { start: number; end: number };
  };
  business: {
    recentAlerts: ProactiveEvent[];
    pendingDecisions: string[];
    upcomingDeadlines: Deadline[];
  };
}
```

#### 預測行動範例

| 情境 | AI 預測 | 信心度 |
|-----|--------|-------|
| 每月 1-5 號早上 | 查看上月營運報表 | 85% |
| 週一早上 | 查看本週重點摘要 | 80% |
| 登入後閒置 30 分鐘 | 輕聲提醒有未讀警報 | 70% |

### Phase E: 個人化 AI 人格

**優先級：** P2  
**預估工作量：** 2 週  
**核心價值：** 讓使用者感覺在與「一個人」互動

```typescript
const DEFAULT_PERSONAS = [
  {
    id: 'executive-advisor',
    name: 'ARIA',
    title: '企業戰略顧問',
    traits: {
      formality: 'semi-formal',
      proactiveness: 'high',
      detailLevel: 'balanced',
    },
    languageStyle: {
      greeting: '早安，董事長',
      selfReference: '我',
      userReference: '您',
    },
    expertise: ['策略規劃', '風險管理', '財務分析'],
  },
];
```

### Token 消耗估算

| 功能 | 月估計 Token/用戶 | 月成本 (Flash) |
|-----|-----------------|---------------|
| 每日簡報 | 60,000 | $0.006 |
| Line 互動 | 120,000 | $0.012 |
| 語音互動 | 180,000 | $0.018 |
| 情境感知 | 45,000 | $0.0045 |
| 事件偵測 | 90,000 | $0.009 |
| **總計** | **495,000** | **$0.05/月** |

**結論：每位使用者每月的額外 AI 成本約 $0.05（約 1.5 元台幣），非常經濟實惠。**

---

## 技術設計

### Master Agent（智能路由）

**職責**：接收使用者輸入，自動判斷該呼叫哪些 Agent 或工具。

```typescript
export class MasterAgent {
    async route(userMessage: string, context: ConversationContext): Promise<ExecutionPlan> {
        // 1. 意圖分析
        const intent = await this.analyzeIntent(userMessage);

        // 2. 能力匹配
        const matchedAgents = this.matchAgents(intent, availableAgents);

        // 3. 編排執行計畫
        return {
            steps: [
                { type: 'agent', agent_id: 'financial-analyst', input: '分析 Q1 財報' },
                { type: 'mcp', server: 'gmail', action: 'send_email', depends_on: [0] }
            ],
            estimated_time: 30,
            requires_confirmation: true
        };
    }
}
```

**智能判斷範例**：

| 使用者輸入 | Master Agent 判斷 |
|-----------|------------------|
| 「分析財報」| 呼叫「財報分析 Agent」|
| 「寄信給客戶」| 呼叫 Gmail MCP |
| 「安排會議並通知團隊」| 1. Google Calendar MCP → 2. Slack MCP |
| 「規劃下季度產品」| 1. 市場研究 Agent → 2. 財務分析 Agent → 3. 產生報告 |

### Agent Protocol（統一介面）

所有 Agent 都必須實作此介面，讓企業自建 Agent、第三方 Agent、MCP Server 都能被統一呼叫：

```typescript
export interface AgentProtocol {
    // 基本資訊
    id: string;
    name: string;
    description: string;
    version: string;
    author: string;

    // 能力描述
    capabilities: Capability[];

    // 執行方法
    execute(input: AgentInput): Promise<AgentOutput>;

    // 健康檢查
    healthCheck(): Promise<boolean>;
}

export interface AgentInput {
    message: string;
    context?: Record<string, any>;
    files?: File[];
    session_id?: string;
}

export interface AgentOutput {
    success: boolean;
    result: any;
    message?: string;
    metadata?: {
        execution_time?: number;
        sources?: string[];
        confidence?: number;
    };
}
```

### 連接器類型

1. **企業 EAKAP Agent**：透過 API 連接知識大腦
2. **第三方 Agent**：本地模組直接執行
3. **MCP Server**：透過 MCP Protocol 連接（Gmail、Slack、Notion 等）

### Agent Store 架構

```typescript
export interface AgentStoreItem {
    id: string;
    name: string;
    description: string;
    author: string;
    author_verified: boolean;

    category: 'productivity' | 'business' | 'communication' | 'data' | 'creative';
    rating: number;
    user_count: number;

    type: 'remote_api' | 'local_module' | 'mcp_server';
    required_permissions: Permission[];

    pricing: {
        type: 'free' | 'freemium' | 'paid' | 'subscription';
        price?: number;
        subscription_price?: number;
    };
}
```

---

## 安全性設計

### API Key 管理

```typescript
export class APIKeyManager {
    async generateAPIKey(userId: string, agentId: string, permissions: string[]): Promise<string> {
        const key = await this.generateSecureKey();
        await this.storeKey({
            user_id: userId,
            agent_id: agentId,
            key: await this.encrypt(key),
            permissions,
            expires_at: addDays(new Date(), 365),
        });
        return key;
    }

    async validateKey(key: string): Promise<ValidationResult> {
        const keyData = await this.getKey(key);
        if (!keyData) return { valid: false, reason: 'Key not found' };
        if (new Date() > keyData.expires_at) return { valid: false, reason: 'Key expired' };
        return { valid: true, user_id: keyData.user_id, permissions: keyData.permissions };
    }
}
```

### 權限控制

- 安裝第三方 Agent 前，檢查所需權限
- 根據權限類型，導向對應的 OAuth 流程
- 支援撤銷權限功能

### Agent 沙盒執行環境

```typescript
export class AgentSandbox {
    async executeInSandbox(agent: Agent, input: AgentInput): Promise<AgentOutput> {
        const sandbox = await this.createSandbox({
            memoryLimit: '512MB',
            cpuLimit: '1 core',
            timeout: 30000,
            networkAccess: agent.config.network_access || false,
            fileSystemAccess: 'read-only'
        });

        try {
            const result = await sandbox.run(agent.execute, input);
            return result;
        } finally {
            await sandbox.cleanup();
        }
    }
}
```

### 資料加密

- 使用 AES-256-GCM 加密敏感資料（API Keys、OAuth Tokens）
- 所有傳輸使用 HTTPS/TLS

---

## 商業模式

### 商業價值關鍵洞察

#### 🔑 洞察一：「大腦」是高黏性、高壁壘的產品

```
企業更換 AI 大腦的成本：

1. 重新上傳所有文件          → 數週工作量
2. 重新訓練向量嵌入          → 數萬 Token 成本
3. 重新設計知識分類          → 組織重新學習
4. 重新建立所有 Agent        → 重複投資
5. 歷史對話記錄遺失          → 知識斷層

結論：一旦企業把「知識大腦」建在您的平台上，
      他們更換的成本極高。這就是您的護城河。
```

#### 🔑 洞察二：「Agent」是高頻變動、低忠誠度的產品

```
企業更換 Agent 的成本：

1. 換一個客服 Bot            → 1 天
2. 換一個報表生成 Agent      → 半天

結論：Agent 是「可替代」的，但如果 Agent 必須連接到
      您的大腦才能運作，您就控制了整個生態系統。
```

#### 🔑 洞察三：平台經濟的「飛輪效應」

```
更多企業購買大腦 → 更多知識被累積 → AI 變得更聰明
         ↑                                    ↓
    吸引更多企業 ← 更豐富的 Agent 生態 ← 更多第三方開發 Agent
```

### AI 大腦平台定價

| 方案 | 月費 | 包含內容 |
|-----|------|---------|
| **基礎方案** | $299/月 | 10GB 儲存、10K 搜尋/月、50K API 呼叫、3 Agent |
| **專業方案** | $799/月 | 50GB 儲存、100K 搜尋/月、200K API 呼叫、10 Agent、API 存取 |
| **企業方案** | $2,499/月 | 200GB 儲存、無限搜尋、無限 API、無限 Agent、SLA 99.9% |

### Agent 應用定價

| Agent 名稱 | 月費 | 功能 |
|-----------|------|------|
| 戰情決策系統 | $199/月 | 儀表板、KPI、風險偵測 |
| 賈維斯語音 | $99/月 | 語音互動、Line 整合 |
| 智能客服 | $149/月 | 網站嵌入式客服 |
| 會議總結 | $79/月 | 會議錄音→摘要→待辦 |
| 自訂 Agent | $49/月/個 | 企業自建的 Agent |

### Agent Hub 訂閱定價

| 功能 | 免費版 | 專業版 $29/月 | 企業版 議價 |
|-----|-------|--------------|------------|
| 連接企業 Agent | 3 個 | 無限 | 無限 |
| 安裝第三方 Agent | 5 個 | 20 個 | 無限 |
| MCP Server 整合 | 3 個 | 10 個 | 無限 |
| 對話記錄保存 | 7 天 | 90 天 | 永久 |
| 每月 AI 呼叫次數 | 100 次 | 1,000 次 | 無限 |
| 團隊協作 | ❌ | 5 人 | 無限 |
| 私有 Agent 商店 | ❌ | ❌ | ✅ |
| API 存取 | ❌ | ✅ | ✅ |

### 收入估算

假設 100 家中小企業客戶：

| 收入項目 | 單價 | 月收入 | 年收入 |
|---------|-----|-------|-------|
| 大腦平台（平均）| $500 | $50,000 | $600,000 |
| 戰情決策系統 | $199 × 60 | $11,940 | $143,280 |
| 賈維斯語音 | $99 × 30 | $2,970 | $35,640 |
| 其他 Agent | $100 × 50 | $5,000 | $60,000 |
| API 超額使用 | $0.001/次 | $3,000 | $36,000 |
| **總計** | - | **$72,910** | **$874,920** |

**對比單體產品模式**：100 客戶 × $399/月 = $39,900/月 = $478,800/年  
**平台模式增加收入：約 83%**

---

## 實施路線圖

### Phase 1: API 層抽離（4-6 週）

**目標：** 將現有功能包裝成可對外呼叫的 API

| 時間 | 任務 |
|-----|------|
| Week 1-2 | 核心 API 設計（OpenAPI 3.0）、認證機制、速率限制 |
| Week 3-4 | 知識 API 實作（documents、search、frameworks）|
| Week 5-6 | Agent API 實作（agents、chat、knowledge）|

### Phase 2: 文件與開發者體驗（2 週）

| 時間 | 任務 |
|-----|------|
| Week 1 | API 文件（Swagger）、使用範例 |
| Week 2 | /developers 頁面、API Key 管理介面 |

### Phase 3: 主動式 AI 體驗（8 週）

| 時間 | 任務 | 優先級 |
|-----|------|-------|
| Week 1-3 | 每日 AI 簡報、通知中心 UI、情境感知歡迎語 | P0 |
| Week 4-7 | Line Bot 架構、緊急警報推送、Line 雙向對話 | P0/P1 |
| Week 8-12 | 語音輸入/輸出、預測行動系統、AI 人格系統 | P1/P2 |

### Phase 4: Agent Hub MVP（3 個月）

| 月份 | 任務 |
|-----|------|
| Month 1 | 統一對話介面、Master Agent 路由、Agent Protocol |
| Month 2 | 企業 EAKAP Agent 連接器、MCP Gmail/Calendar 整合 |
| Month 3 | Agent 商店 UI、API Key 管理、內部測試 |

### Phase 5: 商店功能（2 個月）

| 月份 | 任務 |
|-----|------|
| Month 4 | 第三方 Agent 安裝、上架審核、權限管理與 OAuth |
| Month 5 | 評分評論系統、付費購買流程、開發者後台 |

### Phase 6: 進階功能（3 個月）

| 月份 | 任務 |
|-----|------|
| Month 6-7 | Workflow Builder、團隊協作、對話分享匯出 |
| Month 8 | 行動 App（React Native）、桌面 App（Electron）|

### 總時間表

```
2026 Q1: API 層抽離 + 主動式 AI 基礎 + MVP
├─ Jan: API 設計與實作
├─ Feb: 主動式 AI + Agent Hub 基礎
└─ Mar: Agent Store + 內部測試

2026 Q2: 商店功能 + Line 整合
├─ Apr: 安裝與上架 + Line Bot
└─ May: 社群功能 + 語音系統

2026 Q3: 進階功能
├─ Jun-Jul: Workflow + 團隊協作
└─ Aug: 多平台 App

2026 Q4: 正式發布
├─ Sep: Public Beta
├─ Oct: 優化
├─ Nov: 正式發布
└─ Dec: 市場推廣
```

---

## 風險評估與緩解

| 風險 | 可能性 | 影響 | 緩解策略 |
|-----|-------|-----|---------|
| API 層增加延遲 | 中 | 高 | 批次 API、邊緣快取、WebSocket 串流 |
| 客戶擔心「被拆賣」| 低 | 中 | 提供「全包方案」、既有客戶 Grandfather 條款 |
| 第三方 Agent 品質差 | 高 | 中 | 認證機制、評分系統、官方推薦清單 |
| 技術整合困難 | 中 | 高 | 採用成熟 MCP SDK，提前 POC |
| 使用者學習曲線仍高 | 低 | 高 | 持續 UX 測試，簡化再簡化 |
| 競品搶先推出 | 中 | 中 | 專注差異化（企業整合），快速迭代 |
| 開發資源分散 | 中 | 中 | Phase 1 專注平台、應用可外包、建立範本專案 |

---

## 成功指標與願景

### 成功指標（12 個月）

| 指標 | 目標值 |
|-----|-------|
| API 呼叫量 | 1,000,000 次/月 |
| 第三方開發者註冊 | 50 人 |
| 獨立 Agent 應用數 | 5 個 |
| 平台收入佔比 | 40%（vs Agent 60%）|
| 客戶 NPS | > 50 |
| 使用者感受「AI 主動性」| 高 |
| 每日登入率 | +30% |
| Line 互動次數 | 5 次/天/用戶 |

### 預期效益

#### 6 個月內
- ✅ MVP 上線
- ✅ 100+ 內部測試使用者
- ✅ 10+ 企業 Agent 連接成功
- ✅ 5+ 第三方 Agent 上架
- ✅ 主動式通知系統上線

#### 12 個月內
- ✅ Public Beta 發布
- ✅ 1,000+ 活躍使用者
- ✅ 50+ Agent Store 上架
- ✅ 10+ 付費企業客戶
- ✅ Line Bot 整合完成

#### 24 個月內
- ✅ 正式發布 1.0
- ✅ 10,000+ 活躍使用者
- ✅ 200+ Agent Store 上架
- ✅ 100+ 企業客戶
- ✅ 達成收支平衡

### 長期願景

```
2026 Q1-Q2: 建立 API 層，驗證平台模式
2026 Q3-Q4: 開放第三方開發者，建立生態系統
2027 Q1:    舉辦「EAKAP Agent 開發者大會」
2027 Q2:    推出「EAKAP Agent Store」
2027 H2:    成為台灣中小企業 AI 大腦的首選平台
```

### 最終願景

> **「最好的 AI 不是你去找它，而是它在對的時間、對的地點，主動出現。」**

**3 年後，當企業主管想使用 AI 時，第一個想到的就是 EAKAP Agent Hub。**

就像：
- 想聽音樂 → Spotify
- 想叫車 → Uber
- 想用 AI → **EAKAP Agent Hub**

---

## 附錄：給非工程師的簡單比喻

### 您現在的系統像什麼？

🍔 **漢堡店**

你開了一家漢堡店，賣「漢堡套餐」。套餐包含：漢堡 + 薯條 + 飲料，一個價格全包。

問題：
- 有人只想吃漢堡，不想要飲料
- 有人想要雙倍薯條
- 有人想自己創造新口味
- 你只能賣套餐，很難滿足所有人

### 您想做的改變是什麼？

🏭 **食材供應商 + 🍔 漢堡連鎖店**

你改成：
1. 經營一個「**中央廚房**」（AI 大腦）
   - 提供高品質食材：肉排、生菜、醬料
   - 賣給任何人：自己的店、加盟店、其他餐廳

2. 同時開自己的「**漢堡連鎖店**」（官方 Agent）
   - 用自己的食材做產品
   - 展示食材的品質

3. 讓其他人開店（**第三方 Agent**）
   - 他們用你的食材
   - 你抽取原料費用
   - 生態系統越大，你的中央廚房越值錢

好處：
- ✅ 只想要食材的客戶可以單買
- ✅ 想要全包的客戶可以去你的店
- ✅ 想自己創業的人可以加盟
- ✅ 你控制了最核心的「食材供應」，別人離不開你

---

**文件完成日期：** 2026-01-12  
**版本：** 2.0（整合版）  
**來源文件：**
- EAKAP_AGENT_HUB_STRATEGIC_PLAN.md
- JARVIS_PROACTIVE_AI_EXPERIENCE_ROADMAP.md
- STRATEGIC_ARCHITECTURE_BRAIN_vs_AGENT_ANALYSIS.md

**下次更新：** Phase 1 完成後（預計 2026-03）

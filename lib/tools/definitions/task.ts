import { ToolContext } from '../types';

export async function create_task(_params: Record<string, unknown>, _context: ToolContext) {
    return { message: "Not implemented yet (Phase 3)" };
}

export const createTask = create_task;

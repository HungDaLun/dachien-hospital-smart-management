# Supabase Dashboard - 郵件驗證設定路徑

## 📍 詳細路徑指引

### 方法 1：Authentication → Providers（最常見）

1. **登入 Supabase Dashboard**
   - 網址：https://supabase.com/dashboard/project/vjvmwyzpjmzzhfiaojul
   - 或：https://supabase.com/dashboard → 選擇 "Knowledge Architects"

2. **完整路徑**
   ```
   左側選單
   └── Authentication
       └── Providers (標籤頁)
           └── Email (提供者卡片)
               └── Confirm email (開關)
   ```

3. **視覺化說明**
   - 點擊左側選單的 **"Authentication"**（通常有鑰匙圖示 🔑）
   - 在頁面頂部會看到多個標籤：**Users**, **Policies**, **Providers**, **Settings**, **URL Configuration**
   - 點擊 **"Providers"** 標籤
   - 找到 **"Email"** 提供者卡片（通常顯示為一個大的卡片，有 Email 圖示）
   - 在 Email 提供者卡片中，找到 **"Confirm email"** 開關
   - 將開關切換為 **OFF**（灰色）
   - 點擊卡片底部的 **"Save"** 按鈕

### 方法 2：Authentication → Settings

1. **路徑**
   ```
   左側選單
   └── Authentication
       └── Settings (標籤頁)
           └── Email Auth 區塊
               └── Enable email confirmations (開關)
   ```

2. **步驟**
   - 點擊 **"Authentication"**
   - 點擊 **"Settings"** 標籤
   - 向下滾動找到 **"Email Auth"** 區塊
   - 找到 **"Enable email confirmations"** 選項
   - 將開關切換為 **OFF**

### 方法 3：Project Settings → Authentication

1. **路徑**
   ```
   左側選單
   └── Settings (齒輪圖示 ⚙️)
       └── Authentication
           └── Email confirmations (開關)
   ```

## 🔍 如果仍然找不到

### 檢查項目

1. **確認您有專案管理員權限**
   - 只有專案擁有者或管理員可以修改認證設定

2. **檢查 Supabase 版本**
   - 某些舊版本可能將設定放在不同位置
   - 新版本通常在 Authentication → Providers

3. **查看頁面截圖**
   - 請告訴我您在 Authentication 頁面看到的所有標籤/選項
   - 我可以根據您看到的內容提供更精確的指引

## ✅ 替代方案（已實作）

**好消息！** 我已經在應用程式層面實作了自動確認郵件的功能。

即使 Supabase Dashboard 中要求郵件驗證，系統也會在註冊後自動確認使用者的郵件，使用者不需要點擊郵件連結。

### 實作位置

- 檔案：`app/api/auth/register/route.ts`
- 功能：註冊成功後自動使用 Admin API 確認郵件

### 優點

- ✅ 不需要修改 Supabase Dashboard 設定
- ✅ 自動處理，使用者體驗無縫
- ✅ 向後相容

## 🧪 測試

您可以測試註冊功能，即使 Supabase 要求郵件驗證，系統也會自動處理，使用者可以直接登入（但需要等待管理員審核）。

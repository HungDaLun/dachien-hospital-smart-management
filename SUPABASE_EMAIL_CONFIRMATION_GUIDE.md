# Supabase 關閉郵件驗證 - 詳細路徑指引

## 📍 在 Supabase Dashboard 中的具體路徑

### 方法 1：透過 Authentication → Providers

1. **登入 Supabase Dashboard**
   - 網址：https://supabase.com/dashboard
   - 選擇專案：**Knowledge Architects**

2. **導航路徑**
   ```
   Dashboard → 左側選單 → Authentication → Providers → Email
   ```

3. **具體步驟**
   - 點擊左側選單的 **"Authentication"**
   - 點擊上方的 **"Providers"** 標籤
   - 找到 **"Email"** 提供者
   - 找到 **"Confirm email"** 選項
   - 將開關切換為 **關閉（OFF）**
   - 點擊 **"Save"** 儲存

### 方法 2：透過 Authentication → Settings

1. **導航路徑**
   ```
   Dashboard → 左側選單 → Authentication → Settings
   ```

2. **查找設定**
   - 在 Settings 頁面中尋找 **"Email Auth"** 區塊
   - 找到 **"Enable email confirmations"** 或 **"Confirm email"** 選項
   - 將開關切換為 **關閉**

### 方法 3：透過 Project Settings

1. **導航路徑**
   ```
   Dashboard → 左側選單 → Settings → Authentication
   ```

2. **查找設定**
   - 在 Authentication 設定頁面中尋找郵件驗證相關選項

## 🔍 如果找不到設定的替代方案

### 方案 A：透過應用程式層面處理

即使 Supabase 要求郵件驗證，我們可以在應用程式層面處理：

1. **註冊時使用 Admin API 確認使用者**
   - 在註冊 API 中，註冊後立即使用 Admin API 確認使用者
   - 這樣使用者就不需要點擊郵件連結

2. **修改註冊 API**

讓我檢查是否可以實作這個方案。

### 方案 B：檢查 Supabase 版本

某些 Supabase 版本可能將此設定放在不同位置：
- 舊版本：可能在 Project Settings → Auth
- 新版本：在 Authentication → Providers → Email

## 📝 視覺化路徑

```
Supabase Dashboard
└── Knowledge Architects (專案)
    └── 左側選單
        └── Authentication
            ├── Users (使用者列表)
            ├── Policies (政策)
            ├── Providers (提供者) ← 這裡！
            │   └── Email
            │       └── Confirm email [開關]
            ├── Settings (設定) ← 或這裡！
            │   └── Email Auth
            │       └── Enable email confirmations [開關]
            └── URL Configuration
```

## 🛠️ 如果仍然找不到

請提供以下資訊，我可以幫您進一步查找：
1. 您看到的 Authentication 頁面有哪些標籤/選項？
2. 是否有看到 "Providers" 或 "Settings" 標籤？
3. 您的 Supabase Dashboard 版本（通常在頁面底部顯示）

## ✅ 驗證設定是否生效

設定完成後，可以透過以下方式驗證：

1. **測試註冊**
   - 前往 `/register` 頁面
   - 註冊一個測試帳號
   - 檢查是否收到驗證郵件
   - 如果沒有收到，且可以直接登入，表示設定成功

2. **檢查 auth.users 表**
   ```sql
   SELECT email, email_confirmed_at, created_at
   FROM auth.users
   ORDER BY created_at DESC
   LIMIT 5;
   ```
   - 如果新註冊的使用者 `email_confirmed_at` 不為 NULL，表示已自動確認
